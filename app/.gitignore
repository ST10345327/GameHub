# IDE
.idea/

# Build output in every module
**/build/

# Secrets (never commit these) Never commit your keystore files or passwords to version control. Use a local properties file instead.
.env
**/.env
*.jks
*.keystore
keystore.properties

# Backend
node_modules/