package com.gamehub.app.ui.wishlist

import androidx.compose.runtime.Composable
import com.gamehub.app.R
import com.gamehub.app.ui.components.PlaceholderScreen

/** Placeholder. Phase 4 adds wishlist and favourites (remove, move to library). */
@Composable
fun WishlistScreen() {
    PlaceholderScreen(titleRes = R.string.wishlist_title)
}